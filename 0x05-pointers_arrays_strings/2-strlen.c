#include "main.h"
#include <string.h>
/**
 * _strlen - gets length of string from a pointer
 *  @s: pointer to string
 *  Return: length of string
 */
int _strlen(char *s)
{
	int a;

	a = strlen(s);
	return (a);
}
