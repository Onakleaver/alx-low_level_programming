#include "main.h"
#include <stdio.h>

/**
 * mul -multiplies charcters from input
 * @a: character to be tested
 * @b: character to be tested
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	return (a * b);
}
