#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * main - entry point
 * @argc: prints number of arguments
 * @argv: prints out the commands entered
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	if ( argc != int)
	{
		printf(0);
	}
