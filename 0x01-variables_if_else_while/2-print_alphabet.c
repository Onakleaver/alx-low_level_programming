#include <stdio.h>
#include <stdlib.h>

/**
 * main - Print the lphabet in lower case
 *Return: Always 0
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++ '\n')
		putchar(letter);

	return (0);
}
