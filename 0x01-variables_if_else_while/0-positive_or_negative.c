#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	srand(time(0));
	n = rand() - RAND_MAX / 2;
	int n;

	while (n = rand) 
	{
		printf("value of n: %d\n", n);
		a++;
	}

	return (0);
}
