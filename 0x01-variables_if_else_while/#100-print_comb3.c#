#include<stdio.h>
/**
  *main -Entry point
  *
  *Return:Always (0);
  */
int main(void)
{
	int num;
	int mbur;

	mbur = 48;
	num = 48;
	while (num <= 57)
	{
	  if (num != mbur)
	    {
		while (mbur <= 57)
		{
			putchar(num);
			putchar(mbur);
			putchar(',');
			putchar(' ');
			mbur++;
		}
		num++;
		putchar('\n');

	   }
	}
	return (0);
}
